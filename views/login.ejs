<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - One-Time Secret</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f8f9fa;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        .login-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .auth-container {
            max-width: 420px;
            width: 100%;
            padding: 50px 40px;
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1), 0 2px 8px rgba(0, 0, 0, 0.05);
            position: relative;
            overflow: hidden;
        }
        
        .auth-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #f49e12, #e6890e, #f49e12);
            background-size: 200% 100%;
            animation: shimmer-border 3s ease-in-out infinite;
        }
        
        @keyframes shimmer-border {
            0% { background-position: -200% 0; }
            100% { background-position: 200% 0; }
        }
        
        .login-header {
            text-align: center;
            margin-bottom: 0px;
        }
        
        .login-logo {
            margin-bottom: -20px;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            padding: 20px;
        }
        
        .login-logo::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, rgba(244, 158, 18, 0.1), rgba(230, 137, 14, 0.05));
            border-radius: 50%;
            z-index: -1;
            animation: pulse-glow 3s ease-in-out infinite;
        }
        
        @keyframes pulse-glow {
            0%, 100% { 
                transform: translateX(-50%) scale(1);
                opacity: 0.3;
            }
            50% { 
                transform: translateX(-50%) scale(1.1);
                opacity: 0.5;
            }
        }
        
        .login-logo img {
            height: 120px;
            width: auto;
            filter: drop-shadow(0 8px 16px rgba(0, 0, 0, 0.15));
            background: transparent;
            transition: all 0.3s ease;
            position: relative;
            z-index: 1;
        }
        
        .login-logo img:hover {
            transform: scale(1.05);
            filter: drop-shadow(0 12px 24px rgba(0, 0, 0, 0.2));
        }
        
        .auth-container h2 {
            color: #2c3e50;
            margin: 0;
            font-size: 2em;
            font-weight: 700;
            letter-spacing: -0.5px;
            background: linear-gradient(135deg, #2c3e50, #34495e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .login-subtitle {
            color: #7f8c8d;
            margin-top: 12px;
            font-size: 1em;
            font-weight: 400;
            letter-spacing: 0.3px;
        }
        
        .alert-banner {
            background: linear-gradient(135deg, #fff3cd, #ffeaa7);
            color: #856404;
            padding: 16px 20px;
            border-radius: 10px;
            margin-bottom: 25px;
            border: 1px solid #ffeaa7;
            text-align: center;
            font-size: 0.9em;
            font-weight: 500;
            box-shadow: 0 4px 12px rgba(255, 234, 167, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .alert-banner::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            animation: shimmer-alert 3s ease-in-out infinite;
        }
        
        @keyframes shimmer-alert {
            0% { left: -100%; }
            50% { left: 100%; }
            100% { left: 100%; }
        }
        
        .alert-banner i {
            margin-right: 8px;
            color: #f39c12;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 500;
            font-size: 0.9em;
        }
        
        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }
        
        .input-wrapper i {
            position: absolute;
            left: 12px;
            color: #666;
            font-size: 1em;
            z-index: 1;
        }
        
        .form-group input {
            width: 100%;
            padding: 12px 12px 12px 40px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
            box-sizing: border-box;
            transition: border-color 0.3s ease;
        }
        
        .form-group input:focus {
            outline: none;
            border-color: #f49e12;
            box-shadow: 0 0 0 2px rgba(244, 158, 18, 0.1);
        }
        
        .btn-primary {
            width: 100%;
            padding: 14px;
            background: #f49e12;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-top: 10px;
        }
        
        .btn-primary:hover {
            background: #e6890e;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(244, 158, 18, 0.3);
        }
        
        .btn-primary i {
            font-size: 1em;
        }
        
        .error-message {
            background: #f8d7da;
            color: #721c24;
            padding: 12px 16px;
            border-radius: 6px;
            margin-bottom: 20px;
            border: 1px solid #f5c6cb;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.9em;
        }
        
        .success-message {
            background: #d4edda;
            color: #155724;
            padding: 12px 16px;
            border-radius: 6px;
            margin-bottom: 20px;
            border: 1px solid #c3e6cb;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.9em;
        }
        
        .form-footer {
            text-align: center;
            margin-top: 20px;
            color: #666;
            font-size: 0.85em;
        }
        
        .security-badge {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            background: #f8f9fa;
            color: #666;
            padding: 6px 12px;
            border-radius: 4px;
            font-size: 0.8em;
        }
        
        @media (max-width: 480px) {
            .auth-container {
                margin: 10px;
                padding: 30px 20px;
            }
            
            .login-logo img {
                height: 100px;
            }
            
            .login-logo::before {
                width: 100px;
                height: 100px;
            }
            
            .auth-container h2 {
                font-size: 1.6em;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="login-wrapper">
        <div class="auth-container">
            <div class="login-header">
                <div class="login-logo">
                    <img src="/logon.png" alt="Connexion" />
                </div>
                <h2>Connexion</h2>
                <p class="login-subtitle">Espace d'administration sécurisé</p>
            </div>
            
            <div class="alert-banner">
                <i class="fas fa-shield-alt"></i>
                <strong>Accès Administrateur :</strong> Cette application est réservée aux administrateurs autorisés uniquement.
            </div>
            
            <% if (error) { %>
                <div class="error-message">
                    <i class="fas fa-exclamation-circle"></i>
                    <%= error %>
                </div>
            <% } %>
            
            <% if (success) { %>
                <div class="success-message">
                    <i class="fas fa-check-circle"></i>
                    <%= success %>
                </div>
            <% } %>
            
            <form action="/login" method="POST" id="loginForm">
                <div class="form-group">
                    <label for="identifier">Email ou nom d'utilisateur</label>
                    <div class="input-wrapper">
                        <i class="fas fa-user"></i>
                        <input type="text" id="identifier" name="identifier" placeholder="votre@email.com ou nom_utilisateur" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <div class="input-wrapper">
                        <i class="fas fa-lock"></i>
                        <input type="password" id="password" name="password" placeholder="Votre mot de passe" required>
                    </div>
                </div>
                
                <button type="submit" class="btn-primary" id="loginBtn">
                    <i class="fas fa-sign-in-alt"></i>
                    Se connecter
                </button>
            </form>
            
            
            <div class="form-footer">
                <div class="security-badge">
                    <i class="fas fa-shield-alt"></i>
                    Connexion sécurisée SSL
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Animation simple du bouton de connexion
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            const btn = document.getElementById('loginBtn');
            btn.innerHTML = '<i class="fas fa-spinner"></i> Connexion...';
            btn.disabled = true;
        });
    </script>
</body>
</html> 