<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="https://asshc.stripocdn.email/content/guids/CABINET_8d21bdbffa7aced2045c678a00ffcf59/images/19_DC7.png">
  <title>Votre mot de passe</title>
  <style type="text/css">
    body {
      background: #F6F6F6;
      font-family: arial, 'helvetica neue', helvetica, sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 4px 24px rgba(34,34,34,0.08);
      padding: 40px 30px 30px 30px;
      max-width: 400px;
      width: 100%;
      text-align: center;
    }
    .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px auto;
      padding: 0;
      width: fit-content;
      transition: transform 0.3s ease;
    }
    
    .logo img {
      max-width: 280px;
      height: auto;
      filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
      transition: transform 0.3s ease;
    }
    
    .logo:hover {
      transform: scale(1.05);
    }
    
    .logo:hover img {
      filter: drop-shadow(0 8px 16px rgba(0, 0, 0, 0.15));
    }
    h1 {
      color: #f49e12;
      font-size: 1.5rem;
      margin-bottom: 10px;
    }
    p {
      color: #222222;
      font-size: 1rem;
      margin-bottom: 25px;
    }
    .notice {
      display: none;
      margin-top: 16px;
      padding: 14px 16px 14px 44px; /* space for icon */
      border-radius: 12px;
      font-size: 0.96rem;
      line-height: 1.35;
      text-align: left;
      position: relative;
      box-shadow: 0 8px 22px rgba(0,0,0,0.06);
      border: 1px solid transparent;
      animation: fadeSlideIn 300ms ease-out both;
    }
    .notice::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 14px;
      width: 18px;
      height: 18px;
      transform: translateY(-50%);
      border-radius: 50%;
      background: currentColor;
      -webkit-mask: var(--icon) no-repeat center / 18px 18px;
              mask: var(--icon) no-repeat center / 18px 18px;
    }
    .notice.success {
      display: block;
      color: #1f7a39;
      background: linear-gradient(0deg, rgba(40,167,69,0.10), rgba(40,167,69,0.10)), #ffffff;
      border-color: rgba(40,167,69,0.35);
      --icon: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white"><path d="M9 16.2l-3.5-3.6L4 14l5 5 12-12-1.5-1.4z"/></svg>');
    }
    .notice.error {
      display: block;
      color: #7a1a1a;
      background: linear-gradient(0deg, rgba(220,53,69,0.10), rgba(220,53,69,0.10)), #ffffff;
      border-color: rgba(220,53,69,0.35);
      --icon: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white"><path d="M1 21h22L12 2 1 21zm12-3h-2v-2h2v2zm0-4h-2v-4h2v4z"/></svg>');
    }
    @keyframes fadeSlideIn {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .password-box {
      background: #fffbe6;
      border: 2px solid #f49e12;
      border-radius: 12px;
      padding: 20px;
      margin: 20px 0;
      word-break: break-all;
      position: relative;
    }
    .password-text {
      font-family: 'Fira Mono', 'Consolas', monospace;
      font-size: 1.1rem;
      color: #f49e12;
      font-weight: bold;
      letter-spacing: 0.04em;
      padding-right: 40px;
    }
    .copy-icon {
      position: absolute;
      top: 15px;
      right: 15px;
      background: #f49e12;
      color: #fff;
      border: none;
      border-radius: 50%;
      width: 38px;
      height: 38px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 18px;
      box-shadow: 0 2px 8px rgba(244,158,18,0.10);
      transition: all 0.2s, box-shadow 0.2s;
      outline: none;
      border: 2px solid transparent;
    }
    .copy-icon:hover, .copy-icon:focus {
      background: #222222;
      color: #fff;
      transform: scale(1.12);
      box-shadow: 0 4px 16px rgba(244,158,18,0.18);
      border: 2px solid #f49e12;
    }
    .copy-icon.copied {
      background: #f49e12;
      color: #fff;
      border: 2px solid #222222;
      animation: pop 0.3s;
    }
    @keyframes pop {
      0% { transform: scale(1); }
      50% { transform: scale(1.18); }
      100% { transform: scale(1); }
    }
    .btn {
      background: #f49e12;
      color: #fff;
      border: none;
      border-radius: 15px;
      padding: 12px 0;
      width: 100%;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.2s;
      margin-top: 10px;
      text-decoration: none;
      display: inline-block;
    }
    .btn:hover {
      background: #222222;
    }

  </style>
  <script>
    function copyPassword() {
      const passwordText = document.querySelector('.password-text').textContent;
      navigator.clipboard.writeText(passwordText).then(function() {
        const copyIcon = document.querySelector('.copy-icon');
        copyIcon.innerHTML = '✓';
        copyIcon.classList.add('copied');
        
        setTimeout(function() {
          copyIcon.innerHTML = '📋';
          copyIcon.classList.remove('copied');
        }, 2000);
      }).catch(function(err) {
        console.error('Erreur lors de la copie:', err);
        alert('Erreur lors de la copie du mot de passe');
      });
    }
  </script>
</head>
<body>
  <div class="container">
    <div class="logo">
      <img src="/logo_corporate.png" alt="PEGASE Expert" />
    </div>
    <% if (typeof secret !== 'undefined' && secret) { %>
      <h1>Votre mot de passe</h1>
      <p>Voici le mot de passe qui vous a été transmis :</p>
      <div class="password-box">
        <div class="password-text"><%= secret %></div>
        <button onclick="copyPassword()" class="copy-icon" title="Copier le mot de passe" aria-label="Copier le mot de passe">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect x="5" y="7" width="10" height="10" rx="2" fill="#fff" stroke="#f49e12" stroke-width="2"/>
            <rect x="7" y="3" width="8" height="10" rx="2" fill="#f49e12" stroke="#fff" stroke-width="2"/>
          </svg>
        </button>
      </div>
      <a href="/" class="btn">Demander un autre mot de passe</a>
      <div id="notice" class="notice" role="status" aria-live="polite"></div>
    <% } else { %>
      <h1>Mot de passe expiré ou déjà consulté</h1>
      <p>Ce lien n'est plus valide.<br>Le mot de passe a déjà été consulté ou a expiré.</p>
      <a href="/" class="btn">Retour à l'accueil</a>
    <% } %>
  </div>
  <script>
    // Fonction pour gérer la demande de nouveau mot de passe
    function requestNewPassword(button) {
      // Afficher un message de chargement
      button.textContent = 'Envoi en cours...';
      button.style.opacity = '0.7';
      button.disabled = true;
      const notice = document.getElementById('notice');
      if (notice) { notice.className = 'notice'; notice.textContent = ''; }
      
      fetch('/request-new-password', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        }
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          button.textContent = 'Demande envoyée';
          button.style.background = '#28a745';
          if (notice) {
            notice.className = 'notice success';
            notice.textContent = 'Votre demande a été envoyée. Vous recevrez une réponse dès que possible. Vous pouvez fermer cette page.';
          }
        } else {
          button.textContent = 'Erreur, réessayez';
          button.style.background = '#dc3545';
          button.disabled = false;
          setTimeout(() => {
            button.textContent = 'Demander un autre mot de passe';
            button.style.background = '#f49e12';
            button.style.opacity = '1';
            if (notice) { notice.className = 'notice error'; notice.textContent = 'Une erreur est survenue. Veuillez réessayer.'; }
          }, 3000);
        }
      })
      .catch(error => {
        console.error('Erreur:', error);
        button.textContent = 'Erreur, réessayez';
        button.style.background = '#dc3545';
        button.disabled = false;
        setTimeout(() => {
          button.textContent = 'Demander un autre mot de passe';
          button.style.background = '#f49e12';
          button.style.opacity = '1';
          if (notice) { notice.className = 'notice error'; notice.textContent = 'Une erreur est survenue. Veuillez réessayer.'; }
        }, 3000);
      });
    }

    // Ajouter les event listeners aux boutons
    document.addEventListener('DOMContentLoaded', function() {
      const buttons = document.querySelectorAll('.btn');
      buttons.forEach(button => {
        if (button.textContent === 'Demander un autre mot de passe') {
          button.addEventListener('click', function(e) {
            e.preventDefault();
            const ok = confirm('Voulez-vous demander un autre mot de passe ? Vous recevrez une réponse dès que possible.');
            if (ok) {
              requestNewPassword(this);
            }
          });
        }
      });
    });
  </script>
</body>
</html>
