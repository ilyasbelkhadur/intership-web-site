<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Créer un compte - PEGASE</title>
  <link rel="stylesheet" href="/public/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body { background: #f6f6f6; margin: 0; font-family: Arial, 'Helvetica Neue', Helvetica, sans-serif; }
    .container { background:#fff; max-width: 460px; margin: 90px auto 40px; border-radius: 20px; box-shadow: 0 2px 16px rgba(0,0,0,0.08); padding: 32px; }
    .logo { text-align:center; margin-bottom: 18px; }
    .logo img { height: 48px; }
    h1 { text-align:center; margin: 0 0 8px; color:#222; font-size: 1.6em; }
    .subtitle { text-align:center; color:#666; margin-bottom: 22px; }
    label { display:block; margin-bottom:6px; font-weight:bold; color:#222; }
    .input-group { margin-bottom: 16px; }
    input { width:100%; padding: 12px 12px; border:1px solid #ccc; border-radius: 10px; font-size: 16px; box-sizing: border-box; }
    .actions { margin-top: 12px; display:flex; gap:12px; }
    .btn { flex:1; display:inline-block; padding:12px 0; background:#f49e12; color:#fff; border:none; border-radius: 10px; font-weight:700; cursor:pointer; text-align:center; text-decoration:none; }
    .btn:hover { background:#222; }
    .msg { margin-bottom:16px; padding:12px 16px; border-radius:10px; text-align:center; font-weight:600; }
    .msg.error { background:#ffe0e0; color:#b00020; }
    .msg.success { background:rgba(16,185,129,.1); color:#0f766e; border:1px solid rgba(16,185,129,.2); }
    .help { text-align:center; margin-top: 10px; color:#666; font-size: .95em; }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <div class="container">
    <div class="logo">
      <img src="/regiister.png" alt="PEGASE" />
    </div>
    <h1>Créer un compte</h1>
    <div class="subtitle">Rejoignez PEGASE pour gérer vos mots de passe à usage unique.</div>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="msg error"><%= error %></div>
    <% } %>
    <% if (typeof success !== 'undefined' && success) { %>
      <div class="msg success"><%= success %></div>
    <% } %>

    <form action="/register" method="POST" autocomplete="off">
      <div class="input-group">
        <label for="username">Nom d'utilisateur</label>
        <input type="text" id="username" name="username" placeholder="Votre nom d'utilisateur" required />
      </div>
      <div class="input-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="vous@exemple.com" required />
      </div>
      <div class="input-group">
        <label for="password">Mot de passe</label>
        <input type="password" id="password" name="password" placeholder="Mot de passe" minlength="6" required />
      </div>
      <div class="input-group">
        <label for="confirmPassword">Confirmer le mot de passe</label>
        <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Confirmez le mot de passe" minlength="6" required />
      </div>
      <div class="actions">
        <button type="submit" class="btn"><i class="fas fa-user-plus"></i> Créer le compte</button>
      </div>
    </form>

    <div class="help">
      Vous avez déjà un compte ? <a href="/login">Se connecter</a>
    </div>
  </div>
</body>
</html>


