<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="https://asshc.stripocdn.email/content/guids/CABINET_8d21bdbffa7aced2045c678a00ffcf59/images/19_DC7.png">
  <title>Mot de passe expiré ou déjà consulté</title>
  <style type="text/css">
    body {
      background: #F6F6F6;
      font-family: arial, 'helvetica neue', helvetica, sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 4px 24px rgba(34,34,34,0.08);
      padding: 40px 30px 30px 30px;
      max-width: 400px;
      width: 100%;
      text-align: center;
    }
    .logo {
      margin-bottom: 20px;
    }
    h1 {
      color: #f49e12;
      font-size: 1.5rem;
      margin-bottom: 10px;
    }
    p {
      color: #222222;
      font-size: 1rem;
      margin-bottom: 25px;
    }
    .btn {
      background: #f49e12;
      color: #fff;
      border: none;
      border-radius: 15px;
      padding: 12px 0;
      width: 100%;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.2s;
      margin-top: 10px;
      text-decoration: none;
      display: inline-block;
    }
    .btn:hover {
      background: #222222;
    }
  </style>
</head>
<body>
  <div class="container">
    <img class="logo" src="https://asshc.stripocdn.email/content/guids/CABINET_8d21bdbffa7aced2045c678a00ffcf59/images/19_DC7.png" alt="logo" height="60">
    <h1>Mot de passe expiré ou déjà consulté</h1>
    <p>
      <% if (typeof message !== 'undefined' && message) { %>
        <%= message %>
      <% } else { %>
        Ce lien n'est plus valide.<br>Le mot de passe a déjà été consulté ou a expiré.
      <% } %>
    </p>
    <a href="/" class="btn">Demander un autre mot de passe</a>
  </div>
  <script>
    document.querySelector('.btn').addEventListener('click', function(e) {
      e.preventDefault();
      
      // Afficher un message de chargement
      this.textContent = 'Envoi en cours...';
      this.style.opacity = '0.7';
      
      fetch('/request-new-password', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        }
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          this.textContent = 'Demande envoyée !';
          this.style.background = '#28a745';
          setTimeout(() => {
            window.location.href = '/';
          }, 2000);
        } else {
          this.textContent = 'Erreur, réessayez';
          this.style.background = '#dc3545';
          setTimeout(() => {
            this.textContent = 'Demander un autre mot de passe';
            this.style.background = '#f49e12';
            this.style.opacity = '1';
          }, 3000);
        }
      })
      .catch(error => {
        console.error('Erreur:', error);
        this.textContent = 'Erreur, réessayez';
        this.style.background = '#dc3545';
        setTimeout(() => {
          this.textContent = 'Demander un autre mot de passe';
          this.style.background = '#f49e12';
          this.style.opacity = '1';
        }, 3000);
      });
    });
  </script>
</body>
</html> 